# 16-01 선택 노드 중심 이동

## 목표

다이어그램 밖 도구 버튼에서 현재 선택 노드의 중심 좌표로 화면을 이동합니다.

`diagram.selection.nodes[0]`의 `bounds.center()`를 구하고 코어 DiagramView의 `scrollTo(x, y)`를 호출합니다.
선택이 없으면 루트 노드를 기본 대상으로 사용합니다.

## 검증

루트 선택 후 중심 이동 버튼을 누르면 최근 동작과 자동 검증 상태가 갱신됩니다.
