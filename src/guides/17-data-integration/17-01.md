# 17-01 JSON 데이터 연동

## 목표

루트·카드·자식 정보를 JSON 객체로 관리하고 동일 데이터로 트리와 상세 패널을 만듭니다.

`integratedMockData`는 root와 nodes 배열을 제공하며, 노드 생성은 데이터의 id·parentId·type·좌표만 사용합니다.
화면의 JSON 펼침 영역에서 실제 입력 구조를 확인할 수 있습니다.

## 검증

JSON으로부터 노드 10개와 연결선 9개가 생성됩니다.
