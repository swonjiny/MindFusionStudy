# 15-02 자식·손자 복합 콘텐츠 트리

## 목표

풍부한 HTML 루트 아래에 일반 ShapeNode 자식 3개와 손자 6개를 연결합니다.

## 구조

JSON의 `parentId`를 기준으로 Map에서 부모·자식을 찾고 `createDiagramLink`로 연결합니다.
루트는 ControlNode, 하위 항목은 02단계에서 학습한 ShapeNode를 재사용합니다.

## 정상 결과

노드 10개, 연결선 9개, 깊이 3단계가 표시됩니다.
