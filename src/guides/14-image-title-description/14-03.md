# 14-03 카드 데이터 없음 상태

## 목표

카드 배열이 비어 있을 때 오류나 빈 흰색 박스 대신 명확한 안내를 표시합니다.

## 분기

`cards.length`가 0이면 카드 `map` 결과를 사용하지 않고
`카드 데이터가 없습니다.` 빈 상태 DOM을 렌더링합니다.

## 정상 결과

데이터·카드 DOM은 각각 0개지만 ControlNode HTML 루트와 빈 상태 메시지는 존재합니다.
