# 11-03 이벤트 중복 등록 방지

## 목표

`nodeDomCreated` 또는 초기화 함수가 반복되어도 같은 DOM에 handler가 중복 등록되지 않게 합니다.

## 구현

`WeakSet`에는 이미 처리한 버튼을 기록하고, `Map`에는 cleanup에 필요한 버튼과 handler를
저장합니다. 예제는 같은 연결 함수를 의도적으로 두 번 호출하지만 실제 등록 수는 1회입니다.

## 확인

버튼 한 번 클릭 시 카운트가 정확히 1 증가하고 `리스너 등록 1회`가 표시되어야 합니다.
