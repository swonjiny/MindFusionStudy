# 11-02 노드 이동과 버튼 클릭 분리

## 목표

ControlNode의 빈 영역은 노드 이동에 쓰고 내부 버튼은 클릭 입력만 받게 합니다.

## 핵심

- 버튼에 `data-interactive="true"`를 지정합니다.
- 클릭 handler에서 `stopPropagation()`을 호출합니다.
- 클릭 전후 `node.bounds`를 비교해 버튼 입력이 이동으로 바뀌지 않았는지 확인합니다.

버튼이 아닌 카드 영역을 드래그하면 ControlNode 자체는 계속 이동할 수 있습니다.
