# 13-03 선택 해제 시 크기 복원

## 목표

선택 해제 시 카드 목록을 제거하고 원래 축약 크기로 되돌립니다.

## 구현

`diagram.selection.clear()` 뒤의 `onSelectionChanged`에서 선택 여부가 false이면 축약 HTML과
`Rect(65, 40, 110, 34)`를 다시 적용합니다. 데이터 배열 자체는 변경하지 않습니다.

## 정상 결과

카드 데이터 3개는 유지되고 카드 DOM 0개, 선택 노드 0개, 확장 false가 됩니다.
