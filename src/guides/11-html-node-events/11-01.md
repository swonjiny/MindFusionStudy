# 11-01 HTML 버튼 클릭 이벤트

## 목표

ControlNode 내부 버튼에 표준 DOM `click` 이벤트를 연결합니다.

## 구현

`nodeDomCreated`에서 버튼을 검색하고 `addEventListener`를 호출합니다. 버튼과 handler 쌍은
`Map`에 보관하여 cleanup에서 같은 함수 참조로 `removeEventListener`할 수 있게 합니다.

## 확인

버튼을 누를 때 클릭 횟수와 검증 패널의 이벤트 수가 한 번씩 증가해야 합니다.
