# 16-02 확대·축소·화면 맞춤

## 목표

외부 패널에서 코어 View의 `zoomFactor`와 `zoomToFit()`을 제어합니다.

확대율은 25~200% 범위로 제한합니다. 확대·축소 후 현재 값을 표시하고 화면 맞춤은 전체 트리가 보이는 배율을 계산합니다.

## 검증

확대 시 값이 증가하고 축소 시 감소하며 화면 맞춤 동작이 오류 없이 완료됩니다.
