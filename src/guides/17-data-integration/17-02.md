# 17-02 Mock API 로딩 상태

## 목표

Promise 기반 Mock API의 대기 상태와 성공 전환을 구현합니다.

로딩 중에는 진행 표시와 안내 문구를 보여주고, 응답 후 같은 JSON 생성 경로로 트리를 만듭니다.
로딩 전용 Diagram 구현을 만들지 않습니다.

## 검증

`데이터 로딩 중`에서 `데이터 준비 완료`로 전환되고 최종 노드·연결선 수가 일치해야 합니다.
